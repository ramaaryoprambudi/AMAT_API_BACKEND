# Cloud Run Environment Configuration
# This file will be copied as .env in the Docker container

NODE_ENV=production
PORT=8080

# Database Configuration (Cloud SQL)
# Update these values with your Cloud SQL instance details
DB_HOST=/cloudsql/YOUR_PROJECT_ID:asia-southeast2:personal-finance-db
DB_USER=finance_app
DB_PASSWORD=YOUR_CLOUD_SQL_PASSWORD
DB_NAME=personal_finance
DB_PORT=3306

# JWT Configuration - CHANGE IN PRODUCTION
JWT_SECRET=YOUR_SUPER_SECURE_JWT_SECRET_FOR_CLOUD_RUN_PRODUCTION_2024
JWT_EXPIRES_IN=7d

# CORS Origins - Update with your frontend domains
ALLOWED_ORIGINS=https://your-frontend-domain.com,https://www.your-frontend-domain.com

# Admin Configuration
ADMIN_EMAILS=admin@your-domain.com

# File Upload Settings
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/jpg,image/png,image/gif,application/pdf

# Security Settings
BCRYPT_ROUNDS=12
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_TIME=900000

# Cloud Run specific optimizations
NODE_OPTIONS=--max-old-space-size=512